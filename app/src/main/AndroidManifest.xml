<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" /> <!-- INTERNET 这个权限会附带一个短信的权限，默认是禁止的，不用管 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="AllowBackup,UnusedAttribute">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EquipmentShowOldDateActivity" />
        <activity android:name=".LogData" />
        <activity android:name=".Login" />
        <activity android:name=".ZhuCe" />
        <activity android:name=".SetData" />
        <activity android:name=".LoginYouXiang" />
        <activity android:name=".AddEquipments" />
        <activity android:name=".ReNameEquipment" />
        <activity android:name=".ShareEquipment" />
        <activity android:name=".ShareGetOrFor" />
        <activity android:name=".GetShare" />
        <activity android:name=".AddLD" />
        <activity android:name=".PwUpdata" />
        <activity android:name=".EquipmentInfoActivity" />
    </application>

</manifest>